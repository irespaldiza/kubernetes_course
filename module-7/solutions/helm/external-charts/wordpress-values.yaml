replicaCount: 1

service:
  type: ClusterIP

ingress:
  enabled: true
  ingressClassName: nginx

wordpressUsername: student
wordpressEmail: student@example.local
wordpressBlogName: Kubernetes Course Blog

persistence:
  enabled: true
  size: 2Gi

mariadb:
  auth:
    database: bitnami_wordpress
    username: bn_wordpress
  primary:
    persistence:
      enabled: true
      size: 2Gi
