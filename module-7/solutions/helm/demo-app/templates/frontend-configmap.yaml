apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "demo-app.frontendConfig.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "demo-app.labels" . | nindent 4 }}
data:
  default.conf: |
{{ tpl .Values.frontendNginxConfig . | indent 4 }}
