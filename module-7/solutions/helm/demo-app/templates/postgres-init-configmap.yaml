apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "demo-app.postgresInit.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "demo-app.labels" . | nindent 4 }}
data:
  init.sql: |
{{ .Values.postgresInitSql | indent 4 }}
